export enum Intent {
  Cancel = "cancel",
  ConfirmDelete = "confirm-delete",
  Create = "create",
  Delete = "delete",
  Edit = "edit",
  Save = "save ",
}

export enum Status {
  ConfirmDelete = "confirm-delete",
  Editing = "editing",
  Feedback = "feedback",
}
