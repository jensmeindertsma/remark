export enum Intent {
  Cancel = "cancel",
  Create = "create",
  Delete = "delete",
  ConfirmDelete = "confirm-delete",
  Edit = "edit",
  Save = "save ",
}

export enum Status {
  CreationFeedback = "creation-feedback",
  ConfirmDelete = "confirm-delete",
}
